<div class="subnav">
<%= render_list :class => "nav nav-tabs" do |li|
  li << link_to(t("users.menu.profile"), user_path(@user.domain))
  li << link_to(t("users.menu.topics"), topics_user_path(@user.domain))
  li << link_to(t("users.menu.favorites"), favorites_user_path(@user.domain))
  li << link_to(t("users.menu.notes"), notes_user_path(@user.domain))  
end %>
</div>
